// NOT IMPLEMENTED 
// NOT IMPLEMENTED 
// NOT IMPLEMENTED 
// NOT IMPLEMENTED 
// NOT IMPLEMENTED 
/*
let signUpPage, signIn, json, fetchJson;

const splash = document.querySelector('splash');

signUpPage = () => {
  window.open("./signUp.html")
}

// https://www.w3schools.com/html/html5_webstorage.asp

/*
export default fetchJson = ()=> {
  //let add_user(uname, pword);
  fetch("./database.json")
  .then(response => response.json())
  .then(json => console.log(json));
}

signIn = (uname, pword) => {
  if (uname = json[uname]);
}

signUp = (uname, pword) => {
  json[uname] = {"username": uname, "password": pword,"highScore" = 0}
}
*/



/*
var obj = {
   table: []
}

obj.table.push({id: 1, square:2});
var json = JSON.stringify(obj);

var fs = require('fs');

fs.writeFile('database.json', json, 'utf8', callback);

fs.readFile('database.json', 'utf8', function readFileCallback(err, data){
    if (err){
        console.log(err);
    } else {
    obj = JSON.parse(data); //now it an object
    obj.table.push({id: 2, square:3}); //add some data
    json = JSON.stringify(obj); //convert it back to json
    fs.writeFile('database.json', json, 'utf8', callback); // write it back
}});

*/